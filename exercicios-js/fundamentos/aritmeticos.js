/**
 * Um pouco mais sobre operadores aritméticos
 */
const [a, b, c, d] = [1, 2, 3, 4]
const soma = a + b + c + d
const subtracao = b - a
const multiplicacao = d*c
const divisao = d / b
const modulo = d % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)